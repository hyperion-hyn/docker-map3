filebeat.config:
  modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false

filebeat.overwrite_pipelines: true

filebeat.autodiscover:
  providers:
    - type: docker
      hints.enabled: true

# filebeat.inputs:
#   - type: log
#     paths:
#       - '/var/log/map3/*.log'
#       - '/var/log/nginx/*.log'

processors:
  - add_cloud_metadata: ~

output.elasticsearch:
  hosts: ["13.250.32.100:9200","54.179.189.221:9200"]
  loadbalance: true

# output.logstash:
#   hosts: ["13.250.32.100:8911"]
#   loadbalance: true

#output.elasticsearch:
#  hosts: '${ELASTICSEARCH_HOSTS:elasticsearch:9200}'
#  username: '${ELASTICSEARCH_USERNAME:}'
#  password: '${ELASTICSEARCH_PASSWORD:}'
